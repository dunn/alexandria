<% if document["file_set_iiif_manifest_ssm"].present? %>
  <div class="row zoom">
    <%= openseadragon_picture_tag(
      document["file_set_iiif_manifest_ssm"],
      { data: {
        openseadragon: {
          tileSources: document["file_set_iiif_manifest_ssm"],
          sequenceMode: true
        } } }
    ) %>
  </div>
<% end %>
